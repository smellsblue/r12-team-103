<div class="row-fluid character-attribute">
  <div class="control-group span5 tome-item">
    <label class="control-label character-attribute-label">
      <strong><%= character_attribute.to_s.capitalize %>:</strong>
    </label>

    <div class="character-attribute-value">
      <div class="progress">
        <div id="<%= character_attribute %>-value" class="bar" style="width: <%= @tome.send(character_attribute) || Tome::DEFAULTS[:attribute] %>%;" data-original-value="<%= @tome.send character_attribute %>"></div>
      </div>
    </div>

    <%= render :partial => "edit_button", :object => character_attribute %>
  </div>
</div>
